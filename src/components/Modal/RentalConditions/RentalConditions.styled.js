import styled from 'styled-components';

export const RentalContainer = styled.div`
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
`;

export const RentalItem = styled.p`
  font-size: 12px;
  font-weight: 400;
  line-height: 18px;
  padding: 7px 14px;
  border-radius: 35px;
  background: #f9f9f9;
`;
export const RentalItemNumber = styled.span`
  font-weight: 600;
  color: var(--accent-color);
`;
